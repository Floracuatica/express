<article>
  <h2>{{project.name}}</h2>
  <p>{{project.description}}</p>
  <p><strong>Tecnologías:</strong> {{#each project.tags}}<span class="tag">{{this}}</span>{{/each}}</p>
  <p>
    <a class="btn" href="{{project.repo}}" target="_blank" rel="noopener">Repositorio</a>
    <a class="btn outline" href="{{project.url}}" target="_blank" rel="noopener">Demo</a>
  </p>
  <p><a href="/projects">← Volver a proyectos</a></p>
</article>
